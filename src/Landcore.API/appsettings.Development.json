{
  "Logging": {
    "LogLevel": {
      "Default": "Debug"
    }
  },
  "MongoDb": {
    "ConnectionString": "mongodb://localhost:27017",
    "DatabaseName": "landcore_dev"
  },
  "Jwt": {
    // DEV-ONLY placeholder signing key so `dotnet run` works out of the box locally. Never use
    // this value outside local development, never commit a real secret here — for any real
    // deployment override via `dotnet user-secrets set "Jwt:SigningKey" "<value>"` or the
    // Jwt__SigningKey environment variable instead (see JwtSettings.cs).
    "SigningKey": "dev-only-placeholder-signing-key-DO-NOT-USE-IN-PRODUCTION-32chars-min"
  }
}
